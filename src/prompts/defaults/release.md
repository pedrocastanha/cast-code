## Release Notes Context

When generating release notes:
- Group changes by category: Features, Bug Fixes, Performance, Breaking Changes, Dependencies
- Skip trivial or internal changes (formatting, typos in non-user-facing code)
- Use clear, user-facing language — not internal implementation details
- Each entry: one sentence, imperative mood
- Breaking changes must be at the top with upgrade instructions
